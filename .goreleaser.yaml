version: 2

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/zlliang/chore/internal/buildinfo.Version={{.Version}}

archives:
  - formats: [tar.gz]

changelog:
  use: github-native
